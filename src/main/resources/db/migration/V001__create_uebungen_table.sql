CREATE TABLE UEBUNGEN
(
    id            VARCHAR(1024) PRIMARY KEY,
    name          VARCHAR(40) ,
    geraet        VARCHAR(255),
    muskelgruppe  VARCHAR(255),
    beschreibung  VARCHAR(1024)
);

